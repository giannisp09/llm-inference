apiVersion: v1
kind: Service
metadata:
  name: vllm-inference
  labels: { app: vllm-inference }
spec:
  type: ClusterIP
  selector:
    app: vllm-inference
  ports:
    - { name: http, port: 80, targetPort: http }
